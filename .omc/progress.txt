# Progress Log - Trading Bot Implementation

## Session: 2026-04-16 — c003.md review implementation

### Completed Stories (C003-US-001 through US-009):
- US-001 (C-1): Fixed HMM forward algorithm transmat direction; predict_filtered now uses
  log(transmat_ + 1e-300) with log_alpha_prev[:, None] + log_trans (axis=0 sum).
- US-002 (H-6, H-7): Added predict_filtered input validation (shape/finite), stale
  _previous_proba auto-reset with WARNING, np.linalg.slogdet with sign guard, both
  np.linalg.LinAlgError AND scipy.linalg.LinAlgError caught on full+tied branches,
  scipy.linalg.solve hoisted to module top.
- US-003 (H-5, H-8): train() separates converged vs non-converged candidates; prefers
  converged pool for BIC; calls self.reset_inference_state() before returning.
- US-004 (H-1, H-2): BaseRegimeStrategy.generate_signal returns spec Signal dataclass
  with all Phase-3 fields; StrategyOrchestrator.should_enter_position halves
  position_size_pct AND clamps leverage to 1.0 under uncertainty/flickering.
- US-005 (H-3, H-4): LABEL_TO_STRATEGY dict covers CRASH/STRONG_BEAR/BEAR/WEAK_BEAR/
  NEUTRAL/WEAK_BULL/BULL/STRONG_BULL/EUPHORIA; ADX ±DM computed from raw series via
  np.where masks so both derive before either is assigned.
- US-006: tests/hmm/test_look_ahead.py slices OHLCV (not features), recomputes and
  compares regime at bar T across short/long slices; asserts ±DM never co-fire.
- US-007: tests/hmm/test_hmm_strategy.py covers 3/4/5 regime vol-rank mapping,
  uncertainty mode, 10% rebalance threshold with zero-to-zero guard, LABEL_TO_STRATEGY,
  post-retrain remapping, cache-populated-on-first-call.
- US-008: needs_rebalance guards zero-to-zero; removed rsi_zscore double z-score;
  predict_filtered accepts timestamp kwarg; dead high_low_range column removed.
- US-009: ruff check + ruff format + ty check all green; 47/47 tests pass.

## Session: 2026-04-16 — HMM follow-ups (issue #1)

### Completed Stories (HMM-FU-001 → HMM-FU-003):
- HMM-FU-001: Cached regime-mean lexsort once at train time. _label_regimes now
  populates self._sorted_regime_ids and self._regime_id_to_rank. predict_filtered
  reads O(1) from the cache instead of recomputing np.lexsort per bar. load()
  rebuilds the cache directly from restored model.means_ without overwriting
  the serialised regime_infos. reset_inference_state is untouched (cache is
  model-state, not inference-state). 5 new regression tests in
  TestRegimeRankCache cover cache population, label consistency with the
  expected mapping, no-lexsort-calls-in-hot-path, reset preserves cache, and
  save/load round-trip.
- HMM-FU-002: Removed StrategySignal dataclass from regime_strategies.py plus
  the import and __all__ entry in shared/core/hmm/__init__.py. Grep confirmed
  the only remaining references are historical review notes under agents/review/.
- HMM-FU-003: Full quality gate — ruff check, ruff format --check, ty check,
  and pytest tests/hmm all green. 52/52 tests pass (47 prior + 5 new).

## Session: 2026-04-16 — HMM follow-ups (issue #2)

### Completed Story (HMM-FU-004):
- HMM-FU-004: Extracted HMMEngine._rebuild_regime_rank_cache() helper.
  Both _label_regimes() (at head, using returned sorted_ids for label loop)
  and load() (after model restore) now call it. Single np.lexsort call-site
  inside hmm_engine.py. Helper raises RuntimeError when self.model is None.
  Does NOT call _label_regimes() from load() — that would clobber the 5
  RegimeInfo risk-parameter fields (probability, recommended_strategy_type,
  max_leverage_allowed, max_position_size_pct, min_confidence_to_act)
  restored from pickle. New TestRegimeInfoRoundTrip test locks that
  behaviour by overriding the 5 fields, save/load, and asserting equality.
  Quality gates: ruff check + format + ty + pytest all green. 53/53 tests
  pass (52 prior + 1 new).

### Files Changed:
- shared/core/hmm/hmm_engine.py — cache fields on __init__, populated in
  _label_regimes, consumed in predict_filtered, rebuilt in load().
- shared/core/hmm/regime_strategies.py — deleted StrategySignal.
- shared/core/hmm/__init__.py — dropped StrategySignal import + export.
- tests/hmm/test_hmm_engine.py — added TestRegimeRankCache (5 tests).

### Learnings / Gotchas:
- Rebuilding the cache in load() by calling _label_regimes() would clobber
  the richer regime_infos loaded from the pickle (Phase-3 fields beyond the
  four _label_regimes writes). Recomputing the lexsort directly in load()
  keeps regime_infos intact.
- ty flags attribute access on Optional[GaussianHMM] even under obvious
  training guarantees; the test helper asserts `engine.model is not None`
  before touching .means_ to satisfy the narrowing.

### Files Changed:
- conftest.py (NEW) — prepends project root to sys.path so `from shared.core.hmm ...`
  works with `uv run python -m pytest`.
- shared/core/hmm/hmm_engine.py — C-1 + H-5/6/7/8 fixes.
- shared/core/hmm/feature_engineering.py — ADX ±DM fix, removed rsi_zscore column,
  removed dead high_low_range.
- shared/core/hmm/regime_strategies.py — Signal migration, LABEL_TO_STRATEGY dict,
  uncertainty leverage clamp, rebalance no-op guard.
- shared/core/hmm/__init__.py — export Signal, StrategySignal, BaseRegimeStrategy,
  LABEL_TO_STRATEGY.
- tests/hmm/test_hmm_engine.py — updated feature list (no rsi_zscore), added
  TestForwardAlgorithmDirection (reference posterior + transposed sanity),
  TestPredictFilteredInputValidation, TestTrainResetsInferenceState.
- tests/hmm/test_hmm_strategy.py (NEW) — 6 test classes covering strategy layer.
- tests/hmm/test_look_ahead.py (NEW) — 4 tests slicing OHLCV per HMM.md mandate.

### Learnings / Gotchas:
- Use `uv run python -m pytest` (not `uv run pytest`) so the root conftest runs and
  `shared.*` imports resolve.
- hmmlearn exposes only `predict_proba` (smoothed) publicly; to verify filtered
  (forward-only) output, use a hand-rolled forward recurrence with
  `model._compute_log_likelihood` + `scipy.special.logsumexp`.
- 4-regime vol-rank bucketing: positions [0.0, 1/3, 2/3, 1.0] with thresholds
  <=0.33 / <=0.66 map to low/mid/high — regime at index 1 (vol_fraction=1/3) is
  strictly > 0.33 so it falls into mid; regime at index 2 (vol_fraction=2/3 ≈ 0.6667)
  is strictly > 0.66 so it falls into high. Test expectations must follow this
  partition rather than uniform quartiles.
- scipy.linalg.LinAlgError vs np.linalg.LinAlgError are distinct exception classes;
  mahalanobis solve must catch both plus ValueError for singular covariance matrices.

## Session: 2026-04-05

### Completed Stories:
- Story US-001: Project structure initialized (backtest/, bots/, shared/, tests/, scripts/, dashboard/, reports/), uv setup, all dependencies installed
- Story US-002: Base strategy class and Open Gate strategy implemented (gate detection, breakout, retest, confirmation, stop loss/take profit)
- Story US-003: Backtesting framework with backtesting.py library, BacktestRunner, walk-forward, Monte Carlo simulation
- Story US-004: Data fetchers for yfinance and Polygon.io with abstract base class
- Story US-005: Shared core library with risk manager, execution handler, state store, orchestrator, metrics, alerts
- Story US-006: First trading bot created with config, main entry point, env_loader integration
- Story US-007: Monitoring dashboard (Streamlit), metrics collector, alert system with webhook support
- Story US-008: Unit tests (32 tests passing, 54% coverage: risk_manager 99%, execution 86%, strategy 86-91%)
- Story US-010: Deployment config (Dockerfile.bot, docker-compose.yml), scripts (bot_status.py, emergency_stop.py, pnl_report.py, reset_daily_stats.py, create_bot.py), .env.example, README.md
- Story US-011: Swapped custom BacktestRunner for backtesting.py library - BacktestRunner now uses backtesting.Backtest class, OpenGateBacktestStrategy adapter, plot() method
- Story US-012: Wired vectorbt into optimizer - VectorbtOptimizer with grid_search_vectorbt() for fast sweeps, hybrid_optimize() for combined approach
- Story US-013: Integrated varlock for secure .env management - env.schema with 1Password op:// refs, env_loader.py for secure variable resolution

### New Files Created:
- `backtest/backtesting_runner.py` - BacktestingPyAdapter with OpenGateBacktestStrategy adapter
- `backtest/vectorbt_optimizer.py` - VectorbtOptimizer for fast parameter sweeps (1000+ combos in seconds)
- `scripts/env_loader.py` - Environment variable loader using varlock with 1Password integration
- `env.schema` - Schema file with 1Password references for POLYGON_API_KEY, ALPACA_KEY_ID, ALPACA_SECRET_KEY, IBKR_ACCOUNT, IBKR_PASSWORD

### Updated Files:
- `backtest/backtest.py` - Replaced custom BacktestRunner with backtesting.py library integration
- `backtest/optimize.py` - Added hybrid_optimize() combining vectorbt (fast) and backtesting.py (detailed)
- `bots/nq_open_gate_001/main.py` - Updated to use env_loader for secure variable resolution

### Dependencies:
Runtime: pandas, numpy, vectorbt, backtesting, yfinance, ta, matplotlib, plotly, polygon, alpaca-py, ib-insync
Dev: pytest, pytest-cov, black, ruff, mypy, flask, streamlit, pytest-asyncio

### Test Results:
- 32 tests passing
- 0 tests failing
- 54% overall coverage
- Ruff linting: All checks passed

### Notes:
- Bot directory renamed from nq-open-gate-001 to nq_open_gate_001 (Python module convention)
- All 32 tests pass, no skipped/failed
- US-009 (backtest validation with 2+ years of real data) remains incomplete - requires external API keys
- varlock integration requires .env.schema with actual 1Password op:// refs for each key

---
## Session: 2026-04-16 — HMM-BT-001 historic backtest runner

### Delivered:
- `scripts/hmm_backtest.py` — walk-forward CLI runner (argparse: --ticker/--start/--end/--interval/--warmup-pct)
- `run_walk_forward(df, *, warmup_pct, n_candidates, stability_bars, min_train_bars, bars_per_year, symbol) -> BacktestResult` importable API
- `BacktestResult` dataclass: final_equity, total_return_pct, sharpe, max_drawdown, bar_count, regime_distribution, equity_curve, exposures
- `tests/hmm/test_backtest_runner.py` — 4 tests (well-formed result, warmup-pct validation, OHLCV column validation, no-look-ahead regression)

### Look-ahead invariant:
- Runner ONLY calls `engine.predict_filtered(feature_vec, timestamp=ts)` — never `model.predict` (Viterbi / full-sequence)
- Monkeypatch regression: `monkeypatch.setattr(HMMEngine, "predict", blow_up)` — runner must still complete
- Exposure is derived from bar T and earned on return T→T+1 (no same-bar execution on info already used)

### Quality gates green:
- ruff check scripts tests/hmm: All checks passed
- ruff format --check scripts tests/hmm: 12 files already formatted
- ty check scripts tests/hmm: All checks passed
- pytest tests/hmm: 57 passed in 49.95s (53 prior + 4 new)
- Live smoke: `uv run python scripts/hmm_backtest.py --ticker SPY --start 2020-01-01 --end 2024-12-31 --interval 1d` → exit 0, printed "Final equity/Total return/Sharpe/Max drawdown/Regime distribution"

### Learnings (non-obvious):
- `compute_features` does NOT return raw `atr` or `sma_50` cols — derive atr via `atr_ratio * current_price`, ema50 via `close.rolling(50).mean()` separately
- After 252-window z-score (`normalize_features` shifts by 1), SPY 5Y daily yields only ~499 train bars at 50% warmup → lowered `min_train_bars` default to 200 in the walk-forward runner (engine default stays 252)
- Synthetic pytest fixture uses 1200 bars → passes `min_train_bars=150` to tests to fit within z-score dropout
- `bar_time.to_pydatetime()` only works on `pd.Timestamp` — guard with `isinstance(..., pd.Timestamp)`, else pass `None` (predict_filtered accepts None)

### Incidental lint/type fixes (scope-wide ruff/ty was required by acceptance criteria):
- `scripts/verify_strategy.py`: removed unused `import numpy as np`, reordered stdlib-first
- `scripts/create_bot.py`: extracted `funds_template = TEMPLATE["funds"]; assert isinstance(funds_template, dict)` before `**funds_template` unpack (ty narrowing)
